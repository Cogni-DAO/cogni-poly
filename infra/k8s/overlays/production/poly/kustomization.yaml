# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cogni-production

resources:
  - ../../../base/node-app

namePrefix: poly-

images:
  - name: ghcr.io/cogni-dao/cogni-poly
    newName: ghcr.io/cogni-dao/cogni-poly
    digest: "sha256:4db4619a27a9b222733c976aa9623ae0cf5790ace6f4fe5881d7c1011825b6c5"

patches:
  # Node-specific ConfigMap values.
  # DNS names must match prefixed Service names (namePrefix applies to all resources).
  - target:
      kind: ConfigMap
      name: node-app-config
    patch: |
      - op: replace
        path: /data/NODE_NAME
        value: "poly"
      - op: replace
        path: /data/TEMPORAL_NAMESPACE
        value: "cogni-production"
      - op: replace
        path: /data/LITELLM_BASE_URL
        value: "http://poly-litellm-external:4000"
      - op: replace
        path: /data/TEMPORAL_ADDRESS
        value: "poly-temporal-external:7233"
      - op: replace
        path: /data/REDIS_URL
        value: "redis://poly-redis-external:6379"
      - op: add
        path: /data/NEXTAUTH_URL
        value: "https://poly.cognidao.org"
  # NodePort + instance label on selector (prevents cross-node routing)
  - target:
      kind: Service
      name: node-app
    patch: |
      - op: add
        path: /spec/ports/0/nodePort
        value: 30100
      - op: replace
        path: /spec/ports/0/targetPort
        value: 3100
      - op: add
        path: /spec/selector/app.kubernetes.io~1instance
        value: "poly"
  # Fix secret refs + container port + instance label
  - target:
      kind: Deployment
      name: node-app
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/envFrom/1/secretRef/name
        value: "poly-node-app-secrets"
      - op: replace
        path: /spec/template/spec/initContainers/0/envFrom/1/secretRef/name
        value: "poly-node-app-secrets"
      - op: replace
        path: /spec/template/spec/initContainers/0/envFrom/1/secretRef/name
        value: "poly-node-app-secrets"
      - op: replace
        path: /spec/template/spec/containers/0/ports/0/containerPort
        value: 3100
      - op: replace
        path: /spec/template/spec/containers/0/resources/requests/memory
        value: "384Mi"
      - op: replace
        path: /spec/template/spec/containers/0/resources/limits/memory
        value: "1Gi"
      # Tier 1 sizing: paired heap bump for the 1Gi container limit. Without
      # this, V8 auto-sizes for the base 512Mi cgroup (~256MB heap) and OOMs
      # well below the configured limit. See docs/research/nextjs-node-memory-sizing.md.
      - op: replace
        path: /spec/template/spec/containers/0/env/0/value
        value: "--max-old-space-size=768"
      - op: add
        path: /spec/selector/matchLabels/app.kubernetes.io~1instance
        value: "poly"
      - op: add
        path: /spec/template/metadata/labels/app.kubernetes.io~1instance
        value: "poly"
  # task.0370 step 1: Doltgres knowledge-plane migrator runs as a second initContainer
  # on the poly Deployment. DATABASE_URL maps to DOLTGRES_URL_POLY (separate from
  # the Postgres DATABASE_URL the main container + first initContainer use).
  - target:
      kind: Deployment
      name: node-app
    patch: |
      - op: add
        path: /spec/template/spec/initContainers/-
        value:
          name: migrate-doltgres
          image: ghcr.io/cogni-dao/cogni-poly:placeholder
          command:
            - /bin/sh
            - -c
            - exec node /app/nodes/$(NODE_NAME)/app/migrate-doltgres.mjs /app/nodes/$(NODE_NAME)/app/doltgres-migrations
          envFrom:
            - configMapRef:
                name: poly-node-app-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: poly-node-app-secrets
                  key: DOLTGRES_URL_POLY
          resources:
            requests:
              memory: "384Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"

  # bug.5025 (2026-05-22): no env-specific external-services overrides.
  # The base manifest (`infra/k8s/base/node-app/external-services.yaml`)
  # provides selectorless Services + EndpointSlices pointing at 10.42.0.1
  # (k3s cni0 gateway). Read the base file header for the full rationale.

# bug.5025: `namePrefix: poly-` above rewrites Service names but NOT the
# `kubernetes.io/service-name` label on the paired EndpointSlices. Without
# this fix-up, the EndpointSlices stay labelled `temporal-external` while
# the Service they're meant to back is renamed `poly-temporal-external` —
# the EndpointSlices are orphaned and CoreDNS returns NXDOMAIN. Replacements
# below copy the post-namePrefix Service name into the paired EndpointSlice
# label in lockstep. Apply to every external-services pair.
replacements:
  - source:
      kind: Service
      name: poly-temporal-external
      fieldPath: metadata.name
    targets:
      - select: { kind: EndpointSlice, name: poly-temporal-external-1 }
        fieldPaths:
          - "metadata.labels.[kubernetes.io/service-name]"
  - source:
      kind: Service
      name: poly-postgres-external
      fieldPath: metadata.name
    targets:
      - select: { kind: EndpointSlice, name: poly-postgres-external-1 }
        fieldPaths:
          - "metadata.labels.[kubernetes.io/service-name]"
  - source:
      kind: Service
      name: poly-litellm-external
      fieldPath: metadata.name
    targets:
      - select: { kind: EndpointSlice, name: poly-litellm-external-1 }
        fieldPaths:
          - "metadata.labels.[kubernetes.io/service-name]"
  - source:
      kind: Service
      name: poly-redis-external
      fieldPath: metadata.name
    targets:
      - select: { kind: EndpointSlice, name: poly-redis-external-1 }
        fieldPaths:
          - "metadata.labels.[kubernetes.io/service-name]"
  - source:
      kind: Service
      name: poly-doltgres-external
      fieldPath: metadata.name
    targets:
      - select: { kind: EndpointSlice, name: poly-doltgres-external-1 }
        fieldPaths:
          - "metadata.labels.[kubernetes.io/service-name]"

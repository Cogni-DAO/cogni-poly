name: poly
type: node
port: 3000
node_id: "5ed2d64f-2745-4676-983b-2fb7e05b2eba"
dockerfile: nodes/poly/app/Dockerfile
# bug.0344 — see infra/catalog/operator.yaml for why these fields exist.
image_tag_suffix: "-poly"
migrator_tag_suffix: "-poly-migrate"
# task.0320 — per-env deploy branches (see operator.yaml).
candidate_a_branch: deploy/candidate-a-poly
preview_branch: deploy/preview-poly
production_branch: deploy/production-poly
path_prefix: nodes/poly/
# bug.5002 — per-env public URLs. Verify scripts read these directly
# instead of computing "${node}-${DOMAIN}" with an "if node = operator"
# special-case. Each repo's catalog declares the truth for its nodes;
# no convention-coupling between repos.
public_url:
  candidate-a: https://poly-test.cognidao.org
  preview: https://poly-preview.cognidao.org
  production: https://poly.cognidao.org
